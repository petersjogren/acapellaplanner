# 2026-09-17 — Mark-along phrases

Prepare: Play ghost from 0 (ghost-only, once). Tap New phrase at each line start. Intervals abut; `addPhrase` still writes 2000/2000 pre/post rolls. Drag-to-mark remains for gaps/repair. Click-select and Option-click-play unchanged.

Taps are starts; no tap compensation; no Continue-from-last-phrase. Selecting a phrase during a pass does not stop-commit. Listen must not steal the engine mid-pass. Domain: `markAlong.ts` (pure tap/stop); still writes through `addPhrase`.
