# 2026-09-16 — Alternate tints on neighbouring phrase overlays

Ghost timeline overlays were all `bg-gold/35`, so adjacent phrases looked like one region.

Even/odd by `sortPhrases` start time: `--phrase-overlay` (gold) vs `--phrase-overlay-alt` (gold mixed toward ink). Same 42% opacity. Not slate/forest (section/voice). Not gap-based — every consecutive mark alternates even when there is a breath between them.

No schema, play-window, or list-row change. Overlays stay `pointer-events-none`.
