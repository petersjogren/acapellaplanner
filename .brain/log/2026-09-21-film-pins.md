# Play click-to-pin film camera

`project.filmPins: { id, ghostMs, u }[]` (optional, no schema bump). On Play, click the visible film to pin that ghost time to that content fraction; next play lerps in ghost time and centres the interpolated `u`. No pins = occupied-time uniform, edge-flush. **Clear all** empties the list. Phrase-start crop chips (`filmCue`) removed.
